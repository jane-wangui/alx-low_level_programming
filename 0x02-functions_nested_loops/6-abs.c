#include "main.h"
/**
 * _abs - compute the absolute value of an int
 *
 * @n: Int to make absolute
 *
 * Return: Int ot int tims negative
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
