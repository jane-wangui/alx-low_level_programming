#include <stdio.h>
/**
 * main - program to print the progrma name
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = argc;

	printf("%s\n", argv[0]);
	return (0);
}
